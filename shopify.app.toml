name = "Apex MCP Bridge"
client_id = "a9a5a9324f5d3a2b886c0ed6265d1ad6"
application_url = "https://keystone.apexradius.ai"
embedded = true

[access_scopes]
scopes = "read_customers,write_customers,read_orders,write_orders,read_products"

[auth]
redirect_urls = ["https://keystone.apexradius.ai/api/v1/platform/shopify/callback"]

[webhooks]
api_version = "2024-10"

  # customers/create + orders/create removed — require Protected Customer Data approval
  # (Partner Dashboard → API access → Protected customer data → request access).
  # Re-add once approved.

  [[webhooks.subscriptions]]
  topics = ["app/uninstalled"]
  uri = "https://keystone.apexradius.ai/api/v1/platform/shopify/webhooks/app-uninstalled"

[pos]
embedded = false
